import { RpcHandler } from './rpc-handler.js';

// Export the main handler class
export { RpcHandler };

// Optionally, export types or interfaces if needed for consumers
// export type { RpcHandlerOptions } from './rpc-handler.js';
