{
  "tasks": {
    "start": "deno run --allow-net --allow-read --allow-env --allow-sys=loadavg src/deno-server.ts",
    "dev": "deno run --watch --allow-net --allow-read --allow-env --allow-sys=loadavg src/deno-server.ts",
    "lint": "deno lint",
    "fmt": "deno fmt",
    "test": "deno test --allow-net --allow-read --allow-env" // Assuming tests will be added/adapted here
  },
  "lint": {
    "files": {
      "include": ["src/"]
    },
    "rules": {
      "tags": ["recommended"],
      "exclude": ["no-explicit-any"] // Example: adjust rules as needed
    }
  },
  "fmt": {
    "files": {
      "include": ["src/"]
    },
    "options": {
      "useTabs": false,
      "lineWidth": 120,
      "indentWidth": 2,
      "singleQuote": false, // Deno fmt default
      "proseWrap": "preserve"
    }
  }
  // Add compilerOptions or importMap if needed later
}
