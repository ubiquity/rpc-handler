{
  "$schema": "https://unpkg.com/knip@5/schema.json",
  "entry": [
    "src/index.ts",
    "tests/**/*.test.ts" // Add test files as entry points
  ],
  "project": ["src/**/*.ts"], // Keep project focused on source
  "ignore": ["lib/**"],
  "ignoreDependencies": [
    "prettier" // Ignore prettier for now, assuming external usage
  ]
}
